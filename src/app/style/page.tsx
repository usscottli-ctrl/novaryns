import { StyleClient } from "@/components/style/style-client";
import { BRAND } from "@/lib/brand";

export const metadata = { title: `风格转换 — ${BRAND}` };

export default function StylePage() {
  return <StyleClient />;
}
